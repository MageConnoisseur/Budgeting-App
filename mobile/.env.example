# FastAPI origin (no trailing slash, no /api). Same API the website uses.
# Local API:            http://localhost:8000
# Android emulator:     http://10.0.2.2:8000  (or leave localhost; the app rewrites it)
# Phone on your LAN:    http://YOUR_LAN_IP:8000
# Production (Render):  https://your-service.onrender.com
EXPO_PUBLIC_API_URL=http://localhost:8000
